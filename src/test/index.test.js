// import sum from '../modules/sum';

describe('Jest init test', () => {
  test('Success test', () => {
    // expect(1 + 1).toBe(2);
  });

  test('Success test 2', () => {
    // expect(sum(1, 2)).toBe(3);
  });
});

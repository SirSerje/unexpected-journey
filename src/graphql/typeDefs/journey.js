//TODO: need to stitch > 2 typeDefs

/*
import gql from 'graphql-tag';

const typeDefs = gql`
    type Journey {
        id: ID!
        userId: String
        journey: Object
        character: Object
    }
    type Query {
        getJourney: [Journey]
    }
    type Mutation {
        addJourney(userId: String!, character: Object!, journey: Object!): Journey
    }
`;
export default typeDefs;*/
